# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Explicitly declare text files you want to always be normalized and converted
# to native line endings on checkout.
*.c text
*.h text
*.js text
*.json text
*.md text
*.html text
*.css text

# Declare files that will always have CRLF line endings on checkout.
*.sln text eol=crlf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary

# Additional common binary formats
*.gif binary
*.svg binary
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary

# Ensure package-lock.json always has LF line endings
package-lock.json text eol=lf

# Handle other common text file types
*.yml text
*.yaml text
*.sh text eol=lf
*.bat text eol=crlf
*.ps1 text eol=crlf
